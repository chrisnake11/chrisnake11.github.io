---
title: 分布偏移
published: 2024-08-05T17:22:46Z
description: '机器学习中，在训练数据集和测试数据集中，数据环境的改变，导致特征分布或者标签的条件分布产生了变化。'
image: ''
tags: ['Neural Network']
category: 'Neural Network'
draft: false
---

# 分布偏移

## 1. 协变量偏移

协变量偏移：训练数据和测试数据在（协变量）分布上存在差异。
即：测试集和训练集的特征分布。但是相同的特征下，在训练集和测试集对应的标签条件分布相同。

$$
P_{train}(X) \ne P_{test}(X) \\
P(Y|X)_{train} = P(Y|X)_{test}
$$

举例：
1. 特征分布不同：在训练集上，特征A多一些，在测试集上特征B多一些。
2. 对应的标签条件分布相同：选取特征A，在训练集和测试集上每种相同的标签，对应的概率分布相同，也就是每一种标签的条件概率相同。

## 2. 标签偏移

训练集和测试集在目标变量的分布存在差异，但是在给定标签下的特征分布保持不变。

$$
P_{train}(Y) \ne P_{test}(Y) \\
P(X|Y)_{train} = P(X|Y)_{test}
$$

举例：
1. 标签分布不同：在训练集上，标签1多一些，在测试集上标签2多一些。
2. 给定标签的特征分布相同：选取相同的标签，在训练集上和测试集上，每种相同的特征，对应的概率分布相同，也就是每一种特征的条件概率相同。

> 协变量偏移是，特征分布不同，固定特征，标签分布相同。
> 标签偏移是，  标签分布不同，固定标签，特征分布相同。

## 3. 概念偏移

随着时间或者地理位置的改变，标签的定义会发生变化。特征分布保持不变，但是标签发生了变化。

# 偏移纠正

